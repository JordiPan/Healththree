{% extends 'base.html.twig' %}
{% block nav %}
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <a class="navbar-brand">
            <span class="fa fa-medkit text-danger"></span>
            HealthOne
        </a>
        <button class="navbar-toggler" data-toggle="collapse" data-target="#navbarItems"
                aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarItems">
            <ul class="navbar-nav">
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('dokter_home') }}">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('recipe_table') }}">Recepten</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('doctor_patient_table') }}">Patiënten</a>
                </li>
            </ul>
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}">Uitloggen</a>
                </li>
            </ul>
        </div>
    </nav>
{% endblock %}
{% block body %}
    <div class="jumbotron">
        <h1 class="display-3">Welkom op HealthOne</h1>
        <p class="lead">Dit website is bedoeld voor mensen die te maken hebben met verzekeringen</p>
        <hr class="my-2">
        <p>Welkom, dokter {{ app.user.username }}</p>
    </div>
{% endblock %}
